export type Credentials = {
  email: string;
  password: string;
};

export type LogInData = {
  token: string;
  userId: string;
};

export type UserData = {
  id: string;
  name: string;
  surname: string;
  phoneNumber: string;
  email: string;
  lastUpdate: string;
  creationDate: string;
};

